version: "3.8"
services:
  ssm_backend:
    build: .
    ports: ["8000:8000"]
    volumes: [".:/app"]
  ssm_dashboard:
    build: .
    command: streamlit run interface/app.py --server.port=8501 --server.address=0.0.0.0
    ports: ["8501:8501"]
    volumes: [".:/app"]
    depends_on: [ssm_backend]
