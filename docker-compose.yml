version: '3.8'

services:
  # Сервер дистрибуции данных (FastAPI Backend Layer)
  ssm_backend:
    build: .
    container_name: ssm_backend_container
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    restart: always
    environment:
      - PYTHONUNBUFFERED=1

  # Панель триажа рисков верхнего уровня (Streamlit Frontend Layer)
  ssm_dashboard:
    build: .
    container_name: ssm_dashboard_container
    command: streamlit run interface/app.py --server.port=8501 --server.address=0.0.0.0
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    depends_on:
      - ssm_backend
    restart: always
